<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصة منازل العليين - النظام المتكامل الشامل</title>
    <link href="https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&family=Tajawal:wght@300;500;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        :root { --primary: #0A5C36; --gold: #D4AF37; --cream: #F8F5E6; --white: #FFFFFF; }
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Tajawal', sans-serif; background-color: var(--cream); color: #333; overflow-x: hidden; }
        
        /* الهيدر والشعار المتحرك */
        header { background: linear-gradient(135deg, var(--primary) 0%, #053d23 100%); color: var(--white); padding: 2rem; text-align: center; border-bottom: 5px solid var(--gold); position: relative; }
        .logo-container { height: 100px; display: flex; justify-content: center; align-items: center; }
        .icon-morph { font-size: 3.5rem; color: var(--gold); animation: morphIcon 6s infinite ease-in-out; }
        @keyframes morphIcon {
            0%, 45% { content: "\f02d"; font-family: "Font Awesome 6 Free"; font-weight: 900; transform: rotateY(0deg); }
            50%, 95% { content: "\f66f"; font-family: "Font Awesome 6 Free"; font-weight: 900; transform: rotateY(360deg); }
        }

        .container { max-width: 1000px; margin: 20px auto; padding: 20px; }
        .dashboard-section { display: none; animation: fadeIn 0.4s ease; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

        /* الإحصائيات الحية */
        .stats-bar { display: flex; justify-content: space-around; background: var(--white); margin: -40px 20px 20px; padding: 20px; border-radius: 15px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); position: relative; z-index: 10; }
        .stat-item { text-align: center; }
        .stat-num { font-size: 1.8rem; font-weight: bold; color: var(--primary); display: block; }

        /* بوابات الدخول */
        .portals { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 20px; margin-top: 20px; }
        .portal-card { background: var(--white); border-radius: 20px; padding: 30px; text-align: center; cursor: pointer; transition: 0.3s; box-shadow: 0 5px 15px rgba(0,0,0,0.05); }
        .portal-card:hover { transform: translateY(-10px); border-bottom: 5px solid var(--gold); }
        .portal-icon { font-size: 3rem; color: var(--primary); margin-bottom: 15px; }

        /* العناصر العامة */
        .card { background: white; padding: 20px; border-radius: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.05); margin-bottom: 20px; }
        .btn-back { background: var(--primary); color: white; border: none; padding: 10px 25px; border-radius: 25px; cursor: pointer; margin-bottom: 20px; font-weight: bold; }
        
        /* شجرة الإنجاز */
        .tree-container { text-align: center; padding: 30px 0; }
        .achievement-node { background: var(--white); width: 60px; height: 60px; border-radius: 50%; border: 4px solid var(--gold); margin: 15px auto; display: flex; align-items: center; justify-content: center; font-weight: bold; box-shadow: 0 0 15px rgba(212,175,55,0.3); }

        /* الخريطة الحرارية للمعلم */
        .heatmap-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(120px, 1fr)); gap: 10px; }
        .heatmap-cell { padding: 15px; border-radius: 8px; text-align: center; color: white; font-weight: bold; }
        
        .whatsapp-float { position: fixed; bottom: 20px; left: 20px; background: #25D366; color: white; width: 60px; height: 60px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 30px; box-shadow: 0 5px 20px rgba(0,0,0,0.2); cursor: pointer; }
    </style>
</head>
<body>

<header id="mainHeader">
    <div class="logo-container"><i class="icon-morph fa-solid fa-book-open"></i></div>
    <h1 style="font-family: 'Amiri', serif; font-size: 2.5rem;">منصة منازل العليين</h1>
    <p>بإشراف: علي | حلقة عاصم بن عمر</p>
</header>

<div class="container">
    
    <div id="homePage">
        <div class="stats-bar">
            <div class="stat-item"><span class="stat-num">150</span><span>طالب</span></div>
            <div class="stat-item"><span class="stat-num">850</span><span>صفحة</span></div>
            <div class="stat-item"><span class="stat-num">8</span><span>حلقات</span></div>
        </div>
        <div class="portals">
            <div class="portal-card" onclick="showSection('teacherDash')"><i class="portal-icon fa-solid fa-chalkboard-user"></i><h3>لوحة المعلم</h3></div>
            <div class="portal-card" onclick="showSection('parentDash')"><i class="portal-icon fa-solid fa-user-tie"></i><h3>واجهة ولي الأمر</h3></div>
            <div class="portal-card" onclick="showSection('studentDash')"><i class="portal-icon fa-solid fa-graduation-cap"></i><h3>واجهة الطالب</h3></div>
        </div>
    </div>

    <div id="teacherDash" class="dashboard-section">
        <button class="btn-back" onclick="goHome()">العودة للرئيسية</button>
        <div class="card">
            <h3>🔥 الخريطة الحرارية للحلقات</h3>
            <div class="heatmap-grid">
                <div class="heatmap-cell" style="background:#2ecc71">عاصم بن عمر<br>98%</div>
                <div class="heatmap-cell" style="background:#f1c40f">أبو بكر<br>75%</div>
                <div class="heatmap-cell" style="background:#e74c3c">علي بن أبي طالب<br>60%</div>
            </div>
        </div>
        <div class="card">
            <h3>👥 قائمة الطلاب</h3>
            <table style="width:100%; text-align:right;">
                <tr style="border-bottom:1px solid #eee"><th>الاسم</th><th>المستوى</th></tr>
                <tr><td>أحمد محمد</td><td>ممتاز</td></tr>
                <tr><td>سعد خالد</td><td>جيد جداً</td></tr>
            </table>
        </div>
    </div>

    <div id="parentDash" class="dashboard-section">
        <button class="btn-back" onclick="goHome()">العودة للرئيسية</button>
        <div class="card" style="text-align:center;">
            <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" width="80" style="margin-bottom:10px;">
            <h3>الابن: محمد علي</h3>
            <p style="color:var(--gold)">اللقب: حامل القرآن 🏆</p>
        </div>
        <div class="card">
            <h3>📊 توزيع الإنجاز (دائري)</h3>
            <canvas id="parentChart"></canvas>
        </div>
    </div>

    <div id="studentDash" class="dashboard-section">
        <button class="btn-back" onclick="goHome()">العودة للرئيسية</button>
        <div class="card">
            <h3>📊 لوحة المنافسة (رسم الأعمدة)</h3>
            <canvas id="barChart"></canvas>
        </div>
        <div class="card">
            <h3>🌟 شجرة إنجازاتي</h3>
            <div class="tree-container">
                <div class="achievement-node">ج 30</div>
                <div class="achievement-node">ج 29</div>
                <div class="achievement-node" style="border-color:#ccc; color:#ccc">ج 28</div>
            </div>
        </div>
    </div>

</div>

<div class="whatsapp-float"><i class="fa-brands fa-whatsapp"></i></div>

<script>
    function showSection(id) {
        document.getElementById('homePage').style.display = 'none';
        document.querySelectorAll('.dashboard-section').forEach(s => s.style.display = 'none');
        document.getElementById(id).style.display = 'block';
        if(id === 'parentDash') loadParentChart();
        if(id === 'studentDash') loadBarChart();
    }

    function goHome() {
        document.querySelectorAll('.dashboard-section').forEach(s => s.style.display = 'none');
        document.getElementById('homePage').style.display = 'block';
    }

    function loadParentChart() {
        new Chart(document.getElementById('parentChart'), {
            type: 'doughnut',
            data: { labels:['حفظ','مراجعة','متبقي'], datasets:[{data:[20,5,5], backgroundColor:['#0A5C36','#D4AF37','#eee']}] }
        });
    }

    function loadBarChart() {
        new Chart(document.getElementById('barChart'), {
            type: 'bar',
            data: { 
                labels:['علي','محمد','عبدالله','سعد'], 
                datasets:[{ label:'الصفحات', data:[50,42,30,25], backgroundColor:['#D4AF37','#0A5C36','#0A5C36','#0A5C36'], borderRadius:8 }] 
            }
        });
    }
</script>
</body>
</html>
